# PromptCard Studio for NovelAI v1.1.0

## 版本定位

这是基础画风探索闭环版本。它在保留原有提示词工作台、卡包、图库、生图和发布处理能力的基础上，加入 ArtistPool、基础探索任务、正式筛选和探索图库。

## 新增与改进

- 新增 ArtistPool 导入、规范化、编辑、备份与恢复；恢复备份不会重复创建备份，删除池子时会同时处理其备份。
- 新增基础画风探索任务：任务命名、任务切换、追加轮次、开始/暂停/继续/结束、失败项重试和任务专属候选记录。
- 权重参数改为“最少抽取 ID 数目”，默认 2；每张候选会在最少值到 10 之间随机决定实际抽取数量。目标图片数上限调整为 1000。
- 新增候选预览与初步标记：心动、Treasure、Special、Reject 会作为正式筛选时的参考注释。
- 新增正式筛选模式：复用图库全屏筛选交互，支持按钮与方向键，将结果归档到 Treasure / Special / Reject。
- 新增独立“探索图库”：三类动态牌堆可打开查看，支持跨牌堆发送；Reject 图片可确认后删除。
- 创建画师串 Card 时自动复制演示图到普通图库，并即时同步首页卡包，无需刷新浏览器。
- 统一使用应用内弹窗和提示，整理探索任务顶部固定布局及候选区布局。

## 数据与升级说明

- 探索任务、候选图和 ArtistPool 属于本机数据，不会随代码仓库提交。
- 升级前请保留原项目目录；使用新版本后可通过“设置 → 更新与迁移”导入旧项目数据。
- 本版本压缩包不包含用户卡包、图库、Vibe、配置、探索运行数据、测试缓存或测试结果。

## 当前边界

- 本版本完成 M1 基础探索闭环；工作区 Block / Card Reference 级编辑与完整结果回流仍在后续补齐。
- 深度探索的父本集、交叉/变异、轮次流程和纵向家系图属于后续 M2，不包含在本版本中；审美分支属于 M3。

## 发布包

- `PromptCard-Studio-v1.1.0-win64.zip`

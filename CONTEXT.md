# PromptCard Studio for NovelAI Context

本地优先的 NovelAI 提示词分类管理工具，一切围绕「卡片」：提示词以 PromptCard 为单位分类复用，工作区快速解析/分块/组合，图库负责生成图片的后期分类整理，并支持 NovelAI 生图（单张/批量）与发布前处理（超分降噪/数据抹除/批量重命名）。交付方式：本地 Web 应用，前端构建产物由后端托管。

## Language

**Prompt Workspace (提示词工作区)**:
应用首页与主体页面。用户在这里把卡片和自由文本组合成提示词块列表，支持高亮、拖拽排序和快捷复制；卡片引用实时关联，复制时展开为完整内容。
_Avoid_: 生图区、提示词输入框

**Card (卡片)**:
提示词的最小复用单元，由分类、名称、内容三部分组成，以文本文件存储。内置 8 个默认分类（角色/动作/画师串/负面/质量/场景/表情/服装），用户可自建。
_Avoid_: wildcard、tag（指代已保存的提示词时）

**Category (分类)**:
卡片的组织维度，每张卡属于且仅属于一个分类。分类有可配置的排序与颜色。
_Avoid_: 文件夹（指 UI 上的分类概念时）、type

**Card Reference (卡片引用)**:
工作区中指向某张卡片的块，界面上显示为"分类:名称"，复制或导出时展开为该卡片的完整内容。
_Avoid_: token、引用块

**Block (块)**:
工作区中的可视单元，可以是卡片引用或自由文本。块之间可拖拽排序。
_Avoid_: segment、item

**Zone (区域)**:
提示词工作区的编辑区域。正面区由「提示词工作台」与若干分区（角色 / 动作 / 画师串等，卡片按分类归入对应分区）组成；负面区为「负面」分区，固定在底部；「背面」是独立的自由文本区，不属于分区模型。卡片添加到当前选中分区，各分区分别保存。
_Avoid_: 正面区、负面区、Tab（旧版"正/负面两区"模型的叫法）

**Image Library (图片库)**:
独立的功能板块，对已生成的图片进行瀑布流预览、筛选与分类整理。
_Avoid_: 后期处理

**Library (图库)**:
项目内用于存放和管理生成图片的固定文件夹。图片导入即复制进库，分类即在库内移动文件。目录结构：`library/<前缀>/<前缀>-<日期>/图片`；未评分 = 根目录下按日期目录 `library/<日期>/`。
_Avoid_: images 目录、素材库

**Quick Import (快速导入)**:
通过内置 xlsx 模板（分类/名称/提示词/图片可选）批量导入为卡片：仅第 1 行表头跳过，分类不存在自动创建，同名自动加后缀（1），图片复制进图库未评分目录并设为卡片演示图。
_Avoid_: 批量添加（特指逐个表单填写）

**Batch Generation (批量生成)**:
对接 NovelAI 后，通过卡片组合自动变换提示词并批量生成图片：角色 × 动作 × 画师串（可加自定义维度）组合枚举、每卡系数、总张数实时计算、串行生成、点数停止阈值、请求节流与失败重试、断点续跑（暂停/继续/结束）。

**Generation Parameter Preset (生成参数)**:
单张与批量生成共用的参数集（模型、分辨率、步数、采样器、调度器、种子、负面预设、质量词、Variety 等），自动记忆上次使用；免费档判定：步数 ≤ 28、分辨率不超过竖版 831×1216 及 3 档更小分辨率、单次 1 张时免费。

**Export (导出/复制)**:
把工作区中的块列表序列化为符合 NovelAI 格式的提示词文本：卡片引用展开为完整内容，块之间以英文逗号连接，输出前做格式规范化。它不是简单复制文本，而是最终的整理动作。
_Avoid_: copy、生成结果

**Review Mode (筛选模式)**:
图片库中的评分视图：一次放大查看一张图片，用 Treasure / Fine / Reject / Like 打分。打分只记录标签，不立即移动或删除文件；结束后统一按标签执行移动/删除。
_Avoid_: 筛选（指普通浏览时）、图片筛选

**Score Tag (评分标签)**:
用户在筛选模式中给图片打的临时标签：treasure、fine、reject、like 四种之一。标签只在筛选会话期间有效，结束筛选后按标签移动文件并丢弃标签，不持久化。
_Avoid_: 评分、分类（指卡片分类时）

**Favorites (收藏夹)**:
图库中保存 like 评分图片的固定文件夹（`like/`，兼容旧 `收藏/`），优先级高于任何评分。

**Send to Workspace (发送到工作区)**:
把图片中读取出的正面/负面提示词整体覆盖到工作区对应的区域；执行前必须提示用户该操作会覆盖现有内容。
_Avoid_: 发送到生图区、导入提示词

**Publishing Pipeline (发布处理)**:
图库中针对多张已勾选图片批量执行的发布前处理流程：图库勾选 →「发布处理」把图片复制进独立暂存区（`publish_staging/`，与图库隔离，图库原图不动）→ 在发布处理页面预览/删除/添加暂存图片 → 勾选节点执行。节点固定顺序：**超分降噪 → 恢复原数据 → 数据抹除 → 批量重命名**；每次处理输出到独立文件夹 `outputs/<时间戳>-<随机>/`，完成后可打开输出文件夹。
_Avoid_: 后处理、发布工具

**Super-resolution Denoising (超分降噪)**:
发布处理节点之一（可选）。插件式引擎：内置 Real-ESRGAN（ncnn-Vulkan，可自动下载）与 waifu2x-caffe（本地路径），按可执行文件名自动识别，参数面板按对应引擎清单渲染（模式/降噪等级/放大倍数/分块/计算设备/TTA 等）。引擎输出会抹掉 PNG 元数据，但图片文件名不变。
_Avoid_: 放大、超分

**Metadata Restore (恢复原数据)**:
发布处理节点之一（可选，仅在勾选超分后可用）。超分会在输出时抹掉 PNG 元数据；勾选本节点时，超分前提取 PNG 元数据（提示词/参数/Exif/时间戳等），超分完成后原样写回。与「数据抹除」互斥——抹除会清掉恢复回来的数据。
_Avoid_: 数据恢复、还原信息

**Batch Data Erasure (数据抹除)**:
发布处理节点之一（可选）。清除 PNG 内部元数据块（tEXt/zTXt/iTXt/eXIf/tIME）与 JPEG 的 EXIF/XMP 段，像素内容不变，并自动校验清除结果；未勾选重命名时，文件名同时改为随机中性名，隐藏文件名里携带的提示词信息。
_Avoid_: 批量清除信息、数据清洗

**Rename Template (重命名规则)**:
发布处理节点之一（可选，最后执行）。命名 = 日期（可选，YYYYMMDD）_ 自定义段（可选）_ 随机数字段（6 位），三部分可拖动换序，命名预览实时更新；默认「日期_随机数字段」。随机数字段用于避免同日多次输出重名，不使用固定序号。
_Avoid_: 命名规则、批量改名格式

**Publish Staging (发布暂存区)**:
发布处理专用的独立工作目录 `publish_staging/`。图库图片以硬链接（同卷秒级，失败自动退回复制）进入暂存区，与图库互不影响；运行时的输入/中间产物在 `publish_runs/<run_id>/`（input / meta / .tmp，运行结束后清理），最终结果输出到 `outputs/<时间戳>-<随机>/` 并长期保留。
_Avoid_: 发布文件夹、待发布区

**Card Combination (卡片组合)**:
批量生成时，从若干分类中各选取若干卡片并为每张卡片设定系数，按组合枚举与系数计算总生成张数。系数表示同一张卡片在单个组合中重复生成的次数；总张数 = 各类（选中卡片数 × 系数）的连乘。
_Avoid_: 排列组合、卡片矩阵

# Novelai-prompt-manager Context

本地优先的 NovelAI 提示词管理与图片整理应用：主体是提示词工作区（卡片化组合提示词），另有独立的图片库板块用于对生成图片做后期分类整理。v1 不接入 NovelAI API，以"组合提示词并复制"为交付方式。

## Language

**Prompt Workspace (提示词工作区)**:
应用首页与主体页面。用户在这里把卡片和自由文本组合成提示词块列表，支持高亮、拖拽排序和快捷复制。
_Avoid_: 生图区、提示词输入框

**Card (卡片)**:
提示词的最小复用单元，由分类、名称、内容三部分组成，以文本文件存储。
_Avoid_: wildcard、tag（指代已保存的提示词时）

**Category (分类)**:
卡片的组织维度，每张卡属于且仅属于一个分类。内置分类：角色、动作、质量、综合sfw、综合nsfw，用户可自建分类。
_Avoid_: 文件夹（指 UI 上的分类概念时）、type

**Card Reference (卡片引用)**:
工作区中指向某张卡片的块，界面上显示为"分类:名称"，复制或导出时展开为该卡片的完整内容。
_Avoid_: token、引用块

**Block (块)**:
工作区中的可视单元，可以是卡片引用或自由文本。块之间可拖拽排序。
_Avoid_: segment、item

**Image Library (图片库)**:
独立的功能板块，对已生成的图片进行瀑布流预览与分类整理。
_Avoid_: 后期处理

**Library (图库)**:
项目内用于存放和管理生成图片的固定文件夹。图片导入即复制进库，分类即在库内移动文件。
_Avoid_: images 目录、素材库

**Quick Import (快速导入)**:
将"名称-内容"对应关系的批量数据一次性导入为卡片（具体格式待定）。
_Avoid_: 批量添加（特指逐个表单填写）

**Batch Generation (批量生成)** [未来模块]:
对接 NovelAI 后，通过修改卡片之间的组合自动变换提示词并批量生成图片，可设定张数与点数消耗上限。

**Generation Parameter Preset (生成参数预设)** [未来模块]:
保存一组生图参数（分辨率、采样器、种子等），供图片生成模块使用。

**Export (导出/复制)**:
把工作区中的块列表序列化为符合 NovelAI 格式的提示词文本：卡片引用展开为完整内容，块之间以英文逗号连接，输出前做格式规范化。它不是简单复制文本，而是最终的整理动作。
_Avoid_: copy、生成结果

**Review Mode (筛选模式)**:
图片库中的评分视图：一次放大查看一张图片，用 Treasure / Fine / Reject / Like 打分。打分只记录标签，不立即移动或删除文件；结束后统一按标签执行移动/删除。
_Avoid_: 筛选（指普通浏览时）、图片筛选

**Score Tag (评分标签)**:
用户在筛选模式中给图片打的临时标签：treasure、fine、reject、like 四种之一。标签只在筛选会话期间有效，结束筛选后按标签移动文件并丢弃标签，不持久化。
_Avoid_: 评分、分类（指卡片分类时）

**Favorites (收藏夹)**:
图库中保存 like 评分图片的固定文件夹，优先级高于任何评分。

**Zone (区域)**:
提示词工作区中"正面提示词"与"负面提示词"两个编辑区之一。一次只编辑一个区域，卡片添加到当前选中区域，两个区域分别保存。
_Avoid_: 正面区、负面区、Tab

**Send to Workspace (发送到工作区)**:
把图片中读取出的正面/负面提示词整体覆盖到工作区对应的两个区域；执行前必须提示用户该操作会覆盖现有内容。
_Avoid_: 发送到生图区、导入提示词

**Publishing Pipeline (发布处理)** [预留功能]:
图库中针对多张已勾选图片批量执行的发布前处理流程，包含可选的超分降噪、可选的批量数据抹除与批量重命名。
_Avoid_: 后处理、发布工具

**Batch Data Erasure (批量数据抹除)** [预留功能]:
将图片复制到新位置后，抹除副本的全部元数据；原始图片原地不动。抹除指移除 PNG 中的元数据信息，像素内容不变。
_Avoid_: 批量清除信息、数据清洗

**Super-resolution Denoising (超分降噪)** [预留功能]:
使用 ANR 同款引擎（realcugan / Anime4K / waifu2x）对图片批量放大与降噪。
_Avoid_: 放大、超分

**Card Combination (卡片组合)** [预留功能]:
批量生成时，从若干分类中各选取若干卡片并为每张卡片设定系数，按组合枚举与系数计算总生成张数。系数表示同一张卡片在单个组合中重复生成的次数；总张数 = 各类（选中卡片数 × 系数）的连乘。
_Avoid_: 排列组合、卡片矩阵

**Publish Staging (发布暂存区)** [预留功能]:
发布处理专用的独立工作文件夹。图库图片以复制方式进入暂存区（图库原图不受影响），处理步骤在暂存区内逐步执行并各自输出到对应子文件夹，发布完成后清理暂存副本。
_Avoid_: 发布文件夹、待发布区

**Publish Step Folder (处理步骤文件夹)** [预留功能]:
发布暂存区内每个处理步骤的输入/输出文件夹（如 初始化、数据抹除、超分降噪），每一步从前一文件夹读入、处理、输出到本步骤文件夹。
_Avoid_: 处理目录、中间文件夹

**Rename Template (重命名模板)** [预留功能]:
发布处理中批量重命名的命名规则：`{YYMMDD}{6位序号}_{后缀}`，序号从 114514 递增，忽略原名；后缀默认 "moni"，通常填写角色名。
_Avoid_: 命名规则、批量改名格式
